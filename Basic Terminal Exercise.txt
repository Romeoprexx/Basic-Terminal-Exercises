Part I

1. mkdir first
2. cd first
3. ni person.txt
4. ren person.txt another.txt
5. cp another.txt copy.txt
6. rm copy.txt
7. cd ..
8. cp -r first second
9. rmdir second

Part II

1. a) The man command  the manual of commands. It is equivalent to help in windows C:\> HELP | find /i "ACL"
   b) When you type in man rm, you scroll by using the arrow keys
2. In the man page for ls
   a) the "-l" flag lists files in a list format with more details
   b) the "-a" flag lists all files and folders including hidden ones
3.  curl https://www.google.com > google.html
4.  google.html| more
5.  less -p hplogo google.html
6.  option + left and right keys
7.  control + e
8.  control + a
9.  option + delete
10. The terminal is an application that gives CLI to interact with the Computer. The shell is what handls and processes the commands
11. The absolute path is a path that starts from the root
12. A relative path starts from the current location
13. A flag adds more functions to terminal commands. Examples of flags I used are -r, -rm, and -l flags.
14. The r flag removes files, and the f flag removes it by force
